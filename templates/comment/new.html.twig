{% extends 'base.html.twig' %}

{% block title %}New Comment{% endblock %}

{% block body %}
    <h1>Create new Comment</h1>

    {{ include('comment/_form.html.twig', { 'action': path('comment_new') }) }}

    <a href="{{ path('comment_index') }}">back to list</a>
{% endblock %}
